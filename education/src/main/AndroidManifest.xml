<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jwong.education">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".StudyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/ThemeFullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.clock.ClockHistoryActivity" />
        <activity android:name=".ui.student.StudentActivity" />
        <activity android:name=".ui.student.StudentInfoActivity" />
        <activity android:name=".ui.student.StudentSelectActivity" />
        <activity android:name=".ui.curriculum.CurriculumSelectActivity" />
        <activity android:name=".ui.clock.ClockDetailActivity" />
        <activity android:name=".ui.student.StudentCostNotificationActivity" />


    </application>


</manifest>